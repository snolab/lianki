# unit/pii-scan.test.ts asserts that scripts/pii-scan.ts still matches each
# credential shape, so it necessarily contains credential-shaped fixtures. They
# are fake and never leave the test. Scanning them here only produces a
# guaranteed false positive that blocks every commit touching the file.
unit/pii-scan.test.ts
